<li class="row">
    <div class="col ord-date">${date}</div>
    <div class="col ord-no">${order_no}</div>
    <div class="col u-stime">${start_time}</div>
    <div class="col prd-name">${name}</div>
    <div class="col u-name">${uname}</div>
    <div class="col u-phone">${phone}</div>
    <div class="col u-person">
        <div class="i-select">
        <div class="lb" data-date="${date}" data-oid="${order_id}" data-pid="${person_id}"><span  >${person_name}</span><i class="arr-down"></i></div>
        <div class="cts hide">
            <ol>
            </ol>
        </div>
        </div>
    </div>
</li>

